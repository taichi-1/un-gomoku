# @pkg/shared

Contracts and schemas. Contains only definitions shared between apps.

## Responsibilities

- Type definitions (coordinates, player IDs, state DTOs, etc.)
- WebSocket event names and payload shapes
- Runtime validation schemas
- Shared constants (e.g., `MAX_CANDIDATES`)
